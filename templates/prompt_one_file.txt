You are editing a repo with Docker/Compose and an nginx-based SPA frontend.

Goal: Implement or update exactly ONE file.

Target file: {{PATH}}

Constraints (from spec):
{{SPEC}}

Rules:
- Output only the full, final file content. No commentary. No fences.
- Must be valid for its context (e.g., NGINX server block only).
