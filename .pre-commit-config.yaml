repos:
  - repo: local
    hooks:
      - id: blueprint-audit
        name: blueprint-audit
        entry: python scripts/project_audit.py --strict
        language: system
        pass_filenames: false
