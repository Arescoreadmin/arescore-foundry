name: compose-smoke

on:
  push:
    branches: [ "**" ]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# add to .github/workflows/ci.yml as a separate job
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: actionlint
        run: docker run --rm -v "$PWD":/repo -w /repo rhysd/actionlint:latest -color

  compose-smoke:
    runs-on: ubuntu-latest
    needs: [lint]
    env:
      DOCKER_BUILDKIT: "1"
      COMPOSE_DOCKER_CLI_BUILD: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Ensure docker compose is available
        shell: bash
        run: |
          if ! docker compose version >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y docker-compose-plugin
          fi
          docker compose version

      - name: "Sanity - validate compose files"
        shell: bash
        run: docker compose -f compose.yml -f compose.federated.yml config --quiet

      - name: Build service images
        shell: bash
        run: docker compose -f compose.yml -f compose.federated.yml build

      - name: Up
        shell: bash
        run: docker compose -f compose.yml -f compose.federated.yml up -d --remove-orphans

      - name: Smoke
        shell: bash
        run: bash scripts/smoke_overlay.sh

      - name: Dump logs on failure
        if: failure()
        shell: bash
        run: |
          docker compose -f compose.yml -f compose.federated.yml ps
          docker compose -f compose.yml -f compose.federated.yml logs --no-color > compose-logs.txt || true
          ls -lh compose-logs.txt || true

      - name: Teardown
        if: always()
        shell: bash
        run: docker compose -f compose.yml -f compose.federated.yml down -v
