name: Smoke
on:
  pull_request:
    branches: [ "*" ]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/verify_lock_in.sh
